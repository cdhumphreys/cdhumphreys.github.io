<span class="tag">
    <slot />
</span>
<style>
    .tag {
        font-size: var(--fs-12);

        display: inline-flex;
        padding: var(--spacing-6) var(--spacing-10);

        background-color: rgba(var(--colour-primary-rgb), 0.25);
        color: var(--colour-primary);
        text-align: center;

        border: 1px solid rgba(var(--colour-primary-rgb), 0.75);

        border-radius: var(--radius-full);
    }
</style>
